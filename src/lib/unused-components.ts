/** Names of Astro sections kept in src/components but not mounted on the live landing. */
export const unusedComponentStems = {
  Divider: "Divider",
  EditionPro: "EditionPro",
  HeroWorkflow: "HeroWorkflow",
  MeetMany: "MeetMany",
  MockMany: "MockMany",
} as const;
