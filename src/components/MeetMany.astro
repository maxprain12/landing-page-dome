---
// Meet Many introduction section
---

<section class="meet-many-intro">
  <div class="meet-many-container">
    <div class="paper-cut-card">
      <div class="meet-many-header">
        <div class="many-avatar">
          <img src="/Many.svg" alt="Many" width="50" height="50" />
        </div>
        <div class="meet-many-title-block">
          <h3 class="cartel-title">This is Many</h3>
          <p class="meet-many-subtitle">Your AI companion throughout your entire journey</p>
        </div>
      </div>
      <p class="meet-many-description">
        From your first login, Many is there to guide you. They help you organize,
        discover connections, and unlock insights from your knowledge. Think of
        Many as your research partner who never sleeps, never forgets, and always
        knows exactly where everything is.
      </p>
    </div>
  </div>
</section>

<style>
  .meet-many-intro {
    padding: 80px 40px;
    position: relative;
    min-height: 400px;
    display: flex;
    align-items: center;
    background: var(--color-bg);
    border-top: 1px solid var(--color-border);
  }

  .meet-many-container {
    max-width: 720px;
    margin: 0 auto;
    position: relative;
    z-index: 1;
  }

  .paper-cut-card {
    background: rgba(255, 252, 240, 0.97);
    padding: 2.5rem 3rem;
    box-shadow: 0 4px 24px rgba(0,0,0,0.12), 0 0 0 1px rgba(0,0,0,0.06);
    border-radius: 4px;
    transform: rotate(-0.3deg);
  }

  .cartel-title {
    font-family: var(--font-cartel);
    font-size: 28px;
    font-weight: 700;
    margin-bottom: 8px;
    color: var(--color-text);
  }

  .meet-many-header {
    display: flex;
    align-items: center;
    gap: 20px;
    margin-bottom: 24px;
    flex-wrap: wrap;
  }

  .many-avatar {
    width: 80px;
    height: 80px;
    background: white;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    box-shadow: 0 4px 16px rgba(0,0,0,0.1);
  }

  .many-avatar img {
    width: 50px;
    height: 50px;
  }

  .meet-many-title-block {
    text-align: left;
  }

  .meet-many-subtitle {
    font-size: 16px;
    color: var(--color-text-secondary);
    margin: 0;
  }

  .meet-many-description {
    font-size: 18px;
    line-height: 1.7;
    color: var(--color-text-secondary);
    margin: 0;
  }

  @media (max-width: 768px) {
    .meet-many-intro {
      padding: 60px 20px;
    }

    .paper-cut-card {
      padding: 1.75rem 1.5rem;
      transform: none;
    }

    .meet-many-header {
      flex-direction: column;
    }

    .meet-many-title-block {
      text-align: center;
    }

    .cartel-title {
      font-size: 24px;
    }

    .meet-many-description {
      font-size: 16px;
    }
  }

  @media (max-width: 480px) {
    .meet-many-intro {
      padding: 40px 16px;
      min-height: auto;
    }

    .paper-cut-card {
      padding: 1.25rem 1.25rem;
    }

    .many-avatar {
      width: 64px;
      height: 64px;
    }

    .many-avatar img {
      width: 40px;
      height: 40px;
    }

    .cartel-title {
      font-size: 20px;
    }

    .meet-many-description {
      font-size: 15px;
    }
  }
</style>
