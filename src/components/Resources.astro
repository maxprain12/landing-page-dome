---
const posts = [
  { slug: "post-collage-academic-researchers", titleEs: "Investigadores académicos" },
  { slug: "post-collage-university-students", titleEs: "Estudiantes universitarios" },
  { slug: "post-collage-knowledge-professionals", titleEs: "Profesionales del conocimiento" },
  { slug: "post-collage-writers-journalists", titleEs: "Escritores y periodistas" },
];
---

<section id="recursos" class="resources">
  <div class="container">
    <div class="section-header">
      <span class="section-badge">Recursos</span>
      <h2 class="cartel-title">
        Casos de uso en redes
      </h2>
      <p class="section-subtitle">
        Posts para LinkedIn con el estilo collage: granulado, imágenes de stock y casos de éxito. Descarga y comparte.
      </p>
    </div>

    <div class="resources-grid">
      {
        posts.map((post) => (
          <article class="resource-card">
            <a
              href={`/linkedin-posts/${post.slug}.png`}
              target="_blank"
              rel="noopener"
              class="resource-link"
              title={`Descargar: ${post.titleEs}`}
            >
              <img
                src={`/linkedin-posts/${post.slug}.png`}
                alt={post.titleEs}
                width="600"
                height="750"
                loading="lazy"
              />
              <div class="resource-overlay">
                <span class="resource-title">{post.titleEs}</span>
                <span class="resource-cta">Ver / Descargar imagen</span>
              </div>
            </a>
          </article>
        ))
      }
    </div>
  </div>
</section>

<style>
  .resources {
    padding: var(--section-padding) 0;
    position: relative;
    background: var(--color-bg);
  }

  .cartel-title {
    font-family: var(--font-cartel);
  }

  .section-header {
    text-align: center;
    max-width: 600px;
    margin-inline: auto;
    margin-bottom: 3rem;
  }

  .section-badge {
    display: inline-block;
    background: var(--bg-tertiary);
    border: 1px solid var(--border);
    padding: 0.5rem 1rem;
    border-radius: 100px;
    font-size: 0.85rem;
    color: var(--text-secondary);
    margin-bottom: 1.5rem;
    letter-spacing: 0.05em;
    text-transform: uppercase;
    font-weight: 600;
  }

  .section-header h2 {
    font-size: clamp(2rem, 5vw, 3rem);
    margin-bottom: 1rem;
  }

  .section-subtitle {
    color: var(--text-secondary);
    font-size: 1.1rem;
  }

  .resources-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 1.5rem;
    max-width: 1000px;
    margin-inline: auto;
  }

  .resource-card {
    border-radius: 4px;
    overflow: hidden;
    border: 1px solid var(--color-border);
    box-shadow: 0 2px 14px rgba(0,0,0,0.08), 0 0 0 1px rgba(0,0,0,0.04);
    background: rgba(255, 252, 240, 0.98);
    transition: box-shadow var(--transition-base), transform var(--transition-base);
  }

  .resource-card:hover {
    box-shadow: 0 8px 28px rgba(89, 96, 55, 0.12);
    transform: translateY(-2px);
  }

  .resource-link {
    display: block;
    position: relative;
    color: inherit;
  }

  .resource-link img {
    width: 100%;
    height: auto;
    display: block;
    vertical-align: middle;
  }

  .resource-overlay {
    position: absolute;
    inset: 0;
    background: linear-gradient(to top, rgba(0, 0, 0, 0.6) 0%, transparent 50%);
    display: flex;
    flex-direction: column;
    justify-content: flex-end;
    padding: 1.25rem;
    opacity: 0;
    transition: opacity var(--transition-base);
  }

  .resource-card:hover .resource-overlay {
    opacity: 1;
  }

  .resource-title {
    font-weight: 600;
    font-size: 1.1rem;
    color: #fff;
  }

  .resource-cta {
    font-size: 0.85rem;
    color: rgba(255, 255, 255, 0.9);
    margin-top: 0.25rem;
  }

  @media (min-width: 900px) {
    .resources-grid {
      grid-template-columns: repeat(4, 1fr);
    }
  }

  @media (max-width: 600px) {
    .resources-grid {
      grid-template-columns: 1fr;
      gap: 1rem;
    }
  }
</style>
