# Opcional: URL base del Dome Provider en producción (login, onboarding web).
PUBLIC_DOME_ACCOUNT_URL=

# Opcional: URL completamente calificada al panel de uso/cuotas del usuario en el provider
# (p. ej. https://provider.dome.app/dashboard). Si se define, aparece «Usage & billing» junto a «Cloud account».
PUBLIC_DOME_USER_DASHBOARD_URL=
