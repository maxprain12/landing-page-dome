# Conversión

- CTA primario: GitHub Releases (`links.releases` en `src/data/landing.ts`).
- Login: `PUBLIC_DOME_ACCOUNT_URL` (`.env.example`).
- Blog y manuales: `docs/content.md`. Contacto es `mailto:`. Pricing público es TBA (`noindex`).
- Prueba social → how it works → CTA final.
