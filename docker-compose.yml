services:
  portfolio:
    build: .
    ports:
      - "3000:3000"
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-sf", "http://127.0.0.1:3000/"]
      timeout: 10s
      interval: 30s
      retries: 3
      start_period: 40s
